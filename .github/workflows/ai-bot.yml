name: AI Auto-Coder
on:
  issues:
    types: [opened]

jobs:
  ai-task:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Ask Gemini
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          # Используем путь models/gemini-pro вместо gemini-1.5-flash
          # Это стандарт, который есть в каждом API ключе
          curl "https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=$GEMINI_API_KEY" \
          -H 'Content-Type: application/json' \
          -X POST \
          -d '{
            "contents": [{"parts":[{"text": "Напиши код для задачи: ${{ github.event.issue.title }}"}]}]
          }' > ai_solution.json

      - name: Push response
        run: |
          git config user.name "AI Bot"
          git config user.email "bot@github.com"
          git add ai_solution.json
          git commit -m "Update" || echo "No changes"
          git push
